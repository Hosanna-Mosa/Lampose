/*
 * Route: /earnings/add-method.tsx
 *
 * The screen lives in components/earnings-add-method/. This file stays at its route path and keeps
 * its default export, because with file-based routing the path IS the route --
 * moving or renaming it deletes the screen behind a green build (M5).
 */
export { AddMethodScreen as default } from '@/components/earnings-add-method/AddMethodScreen';
