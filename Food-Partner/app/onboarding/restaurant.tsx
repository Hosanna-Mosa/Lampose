/* Route file. The screen itself lives in components/onboarding-restaurant/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { StepRestaurant } from "@/components/onboarding-restaurant";

export default StepRestaurant;
