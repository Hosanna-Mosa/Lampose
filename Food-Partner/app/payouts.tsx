/* Route file. The screen itself lives in components/dash-payouts/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { DashPayouts } from "@/components/dash-payouts";

export default DashPayouts;
