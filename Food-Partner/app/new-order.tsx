/* Route file. The screen itself lives in components/new-order/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { NewOrderScreen } from "@/components/new-order";

export default NewOrderScreen;
