/* Route file. The screen itself lives in components/dash-orders/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { DashOrders } from "@/components/dash-orders";

export default DashOrders;
