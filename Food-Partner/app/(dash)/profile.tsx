/* Route file. The screen itself lives in components/dash-profile/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { DashProfile } from "@/components/dash-profile";

export default DashProfile;
