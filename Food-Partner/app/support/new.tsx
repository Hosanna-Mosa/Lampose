/* Route file. The screen itself lives in components/support-new/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { NewSupportRequest } from "@/components/support-new";

export default NewSupportRequest;
