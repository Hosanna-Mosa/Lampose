/* Route file. The screen itself lives in components/support-reference/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { SupportThreadScreen } from "@/components/support-reference";

export default SupportThreadScreen;
