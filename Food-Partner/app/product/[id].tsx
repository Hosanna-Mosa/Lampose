/* Route file. The screen itself lives in components/product-id/ — this file
   exists to BE the route, and with file-based routing its path is its route,
   so it never moves and never gets renamed. */
import { ProductScreen } from "@/components/product-id";

export default ProductScreen;
