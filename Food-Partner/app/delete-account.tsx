/* Route file. The screen itself lives in components/delete-account/ — this
   file exists to BE the route, and with file-based routing its path is its
   route, so it never moves and never gets renamed. */
import { DeleteAccountScreen } from "@/components/delete-account/DeleteAccountScreen";

export default DeleteAccountScreen;
