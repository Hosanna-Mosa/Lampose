/* Route file. The screen itself lives in components/logout/ — this file
   exists to BE the route, and with file-based routing its path is its
   route, so it never moves and never gets renamed. */
import { LogoutScreen } from "@/components/logout/LogoutScreen";

export default LogoutScreen;
