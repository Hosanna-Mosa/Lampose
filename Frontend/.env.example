VITE_API_BASE_URL=http://localhost:5000/api

# Places autocomplete + map picker on /food-partner/onboarding.
# Unset simply disables the picker; the rest of the site is unaffected.
VITE_GOOGLE_MAPS_API_KEY=
